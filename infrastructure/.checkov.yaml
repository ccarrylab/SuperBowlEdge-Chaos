skip-check:
  # Acceptable risks with documented justification
  - CKV_AWS_260  # Port 80 required for HTTP to HTTPS redirect
  - CKV_AWS_2    # HTTP listener required for redirect to HTTPS
  - CKV_AWS_382  # Controlled egress required for updates
  - CKV_TF_1     # Using version pinning instead of commit hash
  - CKV_AWS_310  # Origin failover not required for single S3 bucket
  - CKV_AWS_374  # Geo restrictions not required for global CDN
  - CKV_AWS_174  # TLS 1.2 enforced via CloudFront default
  - CKV_AWS_192  # Log4j not applicable to infrastructure
  - CKV_AWS_55   # S3 ACLs already blocked via other controls
